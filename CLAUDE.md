# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 개요

오사카 여행 장소를 지인들과 모아보는 웹앱. 형제 디렉터리 두 개가 같은 문제를 푼다.

| 디렉터리 | 상태 | 포트 |
|---|---|---|
| `osaka_travel_buddy/` | **현재 작업 중인 버전.** 새로 작성 중 | 3100 |
| `osaka_maps_lister/` | 먼저 만든 버전. 참고용으로만 둠 | 3000 |

둘 다 Node ESM + Express 5, **빌드 단계 없음**, 프론트엔드는 번들러 없는 vanilla ES modules (`public/`을 정적 제공). 저장소는 `data/db.json` 단일 JSON 파일.

사용자와의 대화는 한국어. 코드 주석도 한국어로 작성한다.

## 명령어

```bash
cd osaka_travel_buddy
npm run dev          # node --watch, http://localhost:3100
npm start

# 일회성 스크립트 (dotenv 대신 Node 내장 플래그 사용 — 프로젝트 밖 스크립트도 동작)
node --env-file=.env ./script.mjs
```

테스트 프레임워크는 없다. 검증은 임시 `.mjs` 스크립트로 실제 API/브라우저를 두드려서 한다 (아래 참조).

## 알아둬야 할 함정

- **`data/db.json`을 지워도 실행 중인 서버에는 반영되지 않는다.** 저장소를 부팅 시 메모리에 통째로 올린다. 반드시 서버를 재시작할 것.
- **포트 정리는 PowerShell로.** Bash 도구는 호출 간에 셸 상태가 유지되지 않아 `kill %1`이 듣지 않는다.
  `Get-NetTCPConnection -LocalPort 3100 -State Listen | ... Stop-Process -Force`
- **Git Bash에서 `curl -d '{"한글"}'`은 인코딩이 깨진다.** UTF-8 파일에 써서 `--data-binary @file`로 보낼 것.
- **닉네임은 NFC 정규화 필수.** macOS는 한글을 NFD(자모 분리)로 보내고 Windows는 NFC로 보낸다. 정규화하지 않으면 같은 "재혁"이 다른 계정이 된다 (`src/routes/auth.js`).

## 구글 지도 링크 해석 — 이 프로젝트의 핵심

`src/resolve.js` → `src/places.js` → `src/savedList.js`. 실측으로 확인한 사실들이므로 재조사하지 말 것.

**구글 지도 페이지 HTML에는 장소 정보가 전혀 없다.** JS 앱 셸이라 place_id 0개, 좌표 0개, 장소명 0개. HTML 스크래핑 시도는 무의미하며, 예전에 넣었던 폴백은 오히려 엉뚱한 URL을 집어와서 제거했다. 리다이렉트 결과(`res.url`)만 신뢰한다.

**개별 링크 해석 순서** (`resolvePlace`):
1. URL의 `!19s<place_id>` 또는 `?place_id=` → 정확·무료
2. `/maps/place/<이름>/@lat,lng` 파싱 → Text Search + 좌표 검증 (150m 이내면 `high`)
3. 좌표만 있으면 주변 검색 → `low`
4. 전부 실패 → `needsManualSearch: true`로 이름 직접 검색 유도

`maps.google.com/?cid=<숫자>` 형태는 **어떤 방법으로도 place_id를 얻을 수 없다.** 수동 검색으로 넘긴다.

**저장 목록**(`!3e3` 링크)은 헤드리스 브라우저(`playwright-core`, 설치된 Chrome/Edge 채널)로만 읽을 수 있다. 목록 항목이 `<a>`가 아니라 JS `div`라서 링크를 못 얻고 **이름 + 평점 + 분류 텍스트만** 추출 가능하다. 가상 스크롤이므로 끝까지 스크롤해야 전부 로드된다.

**평점이 매칭 검증 신호다.** 이름이 번역되거나 지점명이 달라도 평점이 소수점까지 일치하면 동일 장소로 확정한다. 실측 50곳 중 50곳 매칭 성공. 이 때문에 `SEARCH_FIELDS`에 `places.rating`이 들어 있다.

셀렉터가 깨지면 `src/savedList.js` 상단 `FEED` 상수 한 곳만 고치면 된다. **저장 목록 기능은 Chrome/Edge가 설치된 로컬에서만 동작한다** — 서버 배포 시 이 기능만 실패한다(개별 링크 등록은 정상).

## Places API 사용 규칙

- **등록 시점에 한 번만 호출하고 결과를 `db.json`에 저장한다.** 조회 때 다시 부르지 않는다. 비용과 응답 속도 양쪽 이유.
- `languageCode=ko&regionCode=KR` — 장소명·분류가 한글로 온다. 주소는 장소에 따라 한글/영문이 섞인다(구글 데이터 유무에 따름).
- **저장하는 것은 대표 사진 한 장뿐이고, 리뷰·영업시간·가격대는 받지도 저장하지도 않는다.** `X-Goog-FieldMask`를 좁힐수록 과금 등급이 낮아지므로 필드를 늘리기 전에 정말 화면에 쓰이는지 확인할 것. 리뷰를 다시 넣으면 db.json이 5배로 커진다(실측: 50곳 기준 82KB → 438KB).
- **사진 URL에는 API 키가 들어가므로 브라우저에 직접 내려보내지 않는다.** `serializePlace`가 `photo`를 통째로 제거하고 `hasPhoto` 불리언만 내보내며, 실제 이미지는 `/api/photo/:placeId`가 프록시하고 `data/photos/`에 캐시한다. 미저장 후보용으로는 로그인 필수 + ref 형식 검증이 걸린 `/api/photo/preview?ref=` 가 따로 있다.
- 레코드 형식을 줄일 때는 `placeModel.js`의 `pruneLegacyFields()`에 규칙을 추가한다. 서버 부팅 시 한 번 실행되어 기존 `db.json`을 정리한다.

## 카테고리·지역 분류 (`src/meta.js`)

카테고리는 **`types` 배열을 순서대로 훑어 첫 매칭을 채택**한다. 구글이 구체적인 것부터 주기 때문이다(`ramen_restaurant, noodle_shop, snack_bar, ..., restaurant`). 정규식 부분 매칭으로 바꾸면 라멘집의 `snack_bar`가 술집으로 잡히는 오분류가 재발한다.

지역은 중심점 + 반경 목록(`AREA_POINTS`)에서 최근접 매칭. LLM 없이 결정적으로 판정한다.

카테고리·지역은 한글 고정 enum이며 사용자가 UI에서 직접 바꿀 수 있다.

## 권한 모델 (`osaka_travel_buddy`)

- 인증: 닉네임 + PIN 4자리 → scrypt 해시, HMAC 서명 쿠키. `.env`의 `ADMIN_NICKNAME`과 같은 닉네임으로 가입하면 관리자.
- **장소명·카테고리·지역·메모는 누구나 수정 가능**하고, 변경 이력이 `place.history`에 쌓인다(최근 20건).
- **확정·다녀옴도 누구나 토글 가능** (장소 단위 전역 플래그, 누가 눌렀는지 기록). 좋아요만 유저별.
- **`locked`(고정)는 관리자 전용.** 켜면 수정·삭제·확정·다녀옴이 전부 잠긴다. **좋아요는 잠겨도 허용** — 개인 의견이라 고정 대상이 아니라는 판단.
- 삭제는 등록자 본인 또는 관리자.

`assertEditable()` 한 곳에서 잠금 검사를 하므로, 새 변경 라우트를 추가할 땐 이 헬퍼를 통과시킬 것.

## 프론트엔드 규칙 (`public/`)

번들러가 없으므로 상대 경로 ES import만 사용한다.

**화면은 목록 하나뿐이다. 상세 페이지는 의도적으로 없앴다** — 리뷰와 여러 장의 사진을 걷어내고 "가볍게"로 방향을 잡았기 때문. 카드를 누르면 페이지 이동 없이 시트가 열리고, 거기서 확정·다녀옴·고정·수정·삭제·구글 지도 열기를 모두 처리한다. 상세 페이지나 리뷰를 되살리려면 먼저 사용자에게 확인할 것.

- DOM은 `ui.js`의 `h(tag, props, ...children)`으로 만든다. `h()`는 `false`/`null` 자식을 걸러낸다.
- **`Element.append()`에 조건부 자식을 직접 넘기지 말 것.** `append(cond && node)`는 화면에 `"false"`를 텍스트로 출력한다. `ui.js`의 `mount(parent, ...children)`을 쓸 것.
- 디자인은 Apple 계열. 색·라운드·그림자는 `styles.css` 상단 토큰으로만 정의하고, 라이트/다크는 `prefers-color-scheme`와 `[data-theme]` 양쪽에 정의한다.
- 좁은 화면(≤640px)에서 네비게이션이 두 줄로 접히기 쉽다. 네비에 요소를 추가하면 모바일 폭에서 가로 스크롤이 생기는지 확인할 것.

## 저장소 (`src/store.js`)

어댑터 하나로 로컬은 `data/db.json` 파일, 배포는 Upstash Redis 를 쓴다. `KV_REST_API_URL` 유무로 갈린다.

**Redis 에서는 컬렉션을 배열 하나로 저장하지 않는다.** `users`·`places`·`trash` 는 HASH(`otb:h:<이름>`)이고 필드 키가 레코드 `id` 다. 배열로 통째로 쓰면 두 사람이 동시에 장소를 올렸을 때 나중에 저장한 쪽이 앞사람 것을 지운다(lost update). `categories`·`areas` 는 관리자만 가끔 바꾸므로 배열 그대로 둔다.

어떤 레코드가 바뀌었는지는 **`load()` 시점 스냅샷과 비교해 알아낸다.** 스냅샷은 `Symbol` 키로 `data` 에 숨겨 붙이므로 요청끼리 섞이지 않는다. 덕분에 라우트는 예전처럼 `save(['places'])` 만 호출하면 되고 바뀐 레코드만 `HSET`, 사라진 레코드만 `HDEL` 된다.

- 예전 배열 형식(`otb:places`)이 남아 있으면 첫 `load()` 에서 HASH 로 옮기고 예전 키를 지운다.
- `HGETALL` 은 순서를 보장하지 않아 `createdAt` 으로 다시 정렬한다.
- 같은 레코드를 동시에 고치면 여전히 나중 저장이 이긴다. 이건 의도된 범위다.
- 좋아요만 Redis SET(`otb:likes`) 이라 `SADD`/`SREM` 으로 원자적이다.

검증은 Upstash REST 프로토콜을 흉내낸 임시 HTTP 서버로 한다. **클라이언트가 `Upstash-Encoding: base64` 를 보내므로 문자열 응답을 base64 로 돌려주지 않으면 값이 깨진다** — 가짜 서버를 만들 때 반드시 지킬 것.

## 날씨 (`src/weather.js`)

목록 맨 위 한 줄 바. 출처는 **tenki.jp 오사카시 기타구 10일 예보** (`/forecast/6/30/6200/27127/10days.html`).

**이 페이지는 표가 HTML 에 그대로 들어 있어 브라우저 없이 파싱된다.** 구글 지도와 헷갈리지 말 것 — 여기엔 헤드리스 브라우저가 필요 없다. `<dd class="forecast10days-actab">` 하나가 하루이고 안에 `.days`·`.forecast-telop`·`.high-temp`·`.low-temp`·`.prob-precip`·`.precip` 가 들어 있다. 페이지는 14일치를 주지만 7일만 쓴다.

일본어 예보 문구를 한글로 옮기는 표가 `CONDITIONS`/`CONNECTORS` 다. **긴 것부터 검사해야 한다** — `大雨` 가 `雨` 보다 먼저 잡혀야 「폭우」가 된다. 연결어는 두 종류로 갈린다: `時々`·`一時` 는 앞말을 「맑고」처럼 잇고(`joins: true`), `のち` 는 순서라 「맑음 뒤 흐림」으로 그대로 둔다.

- **남의 서버라 자주 두드리지 않는다.** 인스턴스 메모리에 1시간 캐시하고 응답에 `s-maxage=1800` 을 붙여 CDN 이 대신 받게 한다(사진 프록시와 같은 방식).
- **날씨는 부가 기능이다.** 실패하면 503 을 주고 프론트(`public/weather.js`)는 바를 아예 그리지 않는다. 사이트가 멈추면 안 된다.
- 첫 화면을 붙잡지 않도록 `showWeatherWhenReady()` 가 배경에서 불러오고 도착하면 다시 그린다.
- 셀렉터가 깨지면 `parseForecast()` 의 정규식만 고치면 된다. 파싱 결과가 0건이면 예외를 던져 조용히 빈 화면이 되는 것을 막는다.

## 쓰레기통 (`db().trash`)

**삭제는 되돌릴 수 있다.** `DELETE /api/places/:id` 는 레코드를 지우지 않고 `deletedBy`/`deletedAt` 을 붙여 `db().trash` 로 옮긴다. 좋아요는 그대로 남겨두므로 복원하면 되살아난다.

- `GET /api/places/trash` · `POST /api/places/trash/:id/restore` · `DELETE /api/places/trash/:id`(완전 삭제)
- 권한: 관리자, 등록자, 버린 사람
- 버린 사이에 같은 `googlePlaceId` 가 다시 등록됐으면 복원은 409
- 사진 프록시는 `places` 와 `trash` **양쪽**에서 장소를 찾는다 — 휴지통 목록에도 썸네일이 보여야 하기 때문
- 멤버를 삭제하면 그 사람의 휴지통 항목도 함께 사라진다(복원할 주인이 없어지므로)

## 필터·정렬 (`public/filters.js`)

등록자 / 카테고리 / 지역 / 상태 4축, 축 사이는 AND. **전부 클라이언트에서 처리한다** — 장소가 수백 개 수준이라 서버 왕복이 불필요하고, 지도를 붙일 때 같은 결과를 그대로 쓸 수 있다.

선택 상태는 쿼리스트링(`?cat=음식점|카페·디저트&area=난바·도톤보리`)에 저장되어 새로고침·링크 공유에도 유지된다. 상태는 `Set` 객체로 들고 다니며 제자리에서 변경하므로, `filters` 객체를 복사해서 넘기면 동작하지 않는다.

각 칩에는 "이 항목만 골랐을 때 남는 개수"가 붙고, **0건이 되는 선택지는 칩 자체를 숨긴다**(그룹 전체가 0건이면 그룹도 사라진다). 눌러도 빈 화면만 나오는 선택지를 없애기 위한 것이며, 의도된 동작이다.

필터가 켜진 상태에서 새 장소를 등록하면 그 장소가 안 보일 수 있으므로 `app.js`가 필터를 자동으로 해제한다.

정렬(등록/평점/좋아요/이름 × 오름·내림)도 같은 파일에 있고 `?sort=&dir=` 로 URL에 실린다. `visiblePlaces()` 가 필터 → 정렬 순으로 처리하므로 **`renderList`는 이미 정렬된 배열을 그대로 그린다** — 목록 쪽에서 다시 정렬하지 말 것.

**말줄임은 안쪽 요소에 걸어야 한다.** 카드 메모처럼 패딩이 있는 상자에 `-webkit-line-clamp` 를 직접 걸면 잘린 다음 줄이 패딩 영역으로 비쳐 보인다. 패딩은 바깥(`.card-memo`), 클램프는 안쪽(`.card-memo-text`)에 둔다.

## 검증 방법

정식 테스트 러너가 없으므로, 변경 후에는 임시 스크립트로 실제 동작을 확인한 뒤 스크립트를 지운다(프로젝트에 남기지 않는다).

- **API 통합**: 서버를 띄우고 `fetch`로 라우트를 순서대로 호출, 기대 상태코드와 비교.
- **브라우저 E2E**: `playwright-core`가 이미 의존성에 있다. `chromium.launch({ channel: 'chrome' })`로 실제 Chrome을 띄워 화면을 조작하고 스크린샷을 남겨 눈으로 확인한다. 요소를 클릭하면 화면이 다시 렌더링되므로 **핸들을 재사용하지 말고 매번 셀렉터로 다시 조회**할 것.
- 스크래핑·API 응답 형식을 바꿀 땐 실제 링크로 확인한다. 단위 테스트로는 잡히지 않는 영역이다.

## 지도 (`public/map.js`)

Maps JS API 는 **지도 탭을 처음 열 때만** 불러온다(초기 로딩을 가볍게 유지). 마커 소스는 `applyFilters()` 결과라서 목록과 지도가 자동으로 같은 것을 보여준다.

마커는 `google.maps.Marker` + SVG data URI 아이콘이다. `AdvancedMarkerElement` 를 쓰려면 Map ID 가 필요해서 설정 부담을 피했다 — 색은 올린 사람 색, 확정은 테두리 굵게, 다녀옴은 반투명.

주의: `setMap(null)` 로 마커를 지워도 **구글이 `<img>` 노드를 DOM 에 남겨둔다.** 마커 수를 세어 검증할 때 DOM 노드를 세면 실제보다 많이 나오므로(실측 52개 마커 → img 93개) `markers.length` 를 봐야 한다. 테스트용으로 `window.__otbMarkers` 에 실제 개수를 노출해 둔다.

`draw()` 는 세대 번호로 동시 호출을 막는다. 이 가드가 없으면 Maps API 로딩을 기다리는 사이 두 호출이 각각 마커를 추가해 중복이 생긴다.

## 관리자 패널 (`src/routes/admin.js`, `public/admin.js`)

`GET/PATCH/DELETE /api/admin/users` — 닉네임·색상 변경, 관리자 지정/해제, 정지/해제, 삭제.

**멤버를 삭제하면 그 사람이 올린 장소와 좋아요가 함께 사라진다.** UI 에서 삭제 전에 장소 개수를 경고에 넣는다.

서버가 **자기 자신에 대한 권한 해제·정지·삭제를 거부한다** — 아무도 관리할 수 없는 상태를 막기 위한 것이라 프론트에서만 숨기지 말 것.

## 모바일 우선

**이 사이트는 주로 휴대폰에서 쓰인다.** 화면을 건드렸다면 iPhone 폭(390px)에서 반드시 확인할 것.

- **입력 요소는 모바일에서 16px 미만이면 안 된다.** iOS Safari 가 포커스 시 화면을 확대해버리고 되돌아오지 않는다. `@media (max-width: 640px)` 에 일괄 지정돼 있다.
- **터치 대상은 40px 이상.** 좁은 화면용 오버라이드가 media query 에 모여 있다.
- 좁은 화면에서는 네비에 **제목·추가·아바타만** 남기고 휴지통·관리·로그아웃은 아바타를 눌러 여는 프로필 시트로 옮긴다(`.nav-only-wide` 로 숨김).
- 필터는 모바일에서 기본으로 접힌다(`.filters.collapsed` + `.filter-toggle`). 안 그러면 첫 화면이 필터로 가득 차 목록이 안 보인다.
- 시트는 모바일에서 화면 하단에 붙는다(한 손 조작).
- **수치 검사만으로는 부족하다.** 가로 스크롤·터치 크기가 모두 통과해도 제목이 `오사...`로 잘리거나 버튼 글자가 세로로 쪼개질 수 있다. 스크린샷을 눈으로 볼 것.

## 배포 (Vercel + Upstash Redis)

`DEPLOY.md` 에 단계별 절차가 있다. 요점만:

- **서버리스에 올라간다.** 파일 쓰기가 안 되므로 저장소를 Redis 로 두었다(`src/store.js` 어댑터). 예전에 "Vercel 에는 못 올린다"고 적어뒀던 것은 틀렸다 — 저장소를 바꿔서 해결했다.
- `api/index.js` 가 진입점이고 `vercel.json` 이 모든 경로를 그리로 넘긴다. 리전은 도쿄(`hnd1`).
- 저장목록 가져오기는 `@sparticuz/chromium` 으로 동작한다. `savedList.js` 의 `launch()` 가 세 갈래다: Vercel → 서버리스 chromium, `CHROMIUM_PATH` → 그 바이너리, 그 외 → 로컬 Chrome/Edge.
- **`SESSION_SECRET` 은 반드시 넣어야 한다.** 없으면 인스턴스마다 임시 키를 따로 만들어서, 로그인한 뒤 다음 요청이 다른 인스턴스로 가면 `unauthorized` 가 뜬다. 재시작마다 로그아웃되는 정도가 아니라 **로그인 자체가 유지되지 않는다.**
- 환경변수는 **저장만으로 반영되지 않는다.** Redeploy 해야 한다. `/api/health` 가 빠진 변수와 이름이 틀린 변수를 짚어준다.
- 배포 후 **브라우저 키에 새 도메인을 등록**해야 지도가 뜬다.
- `Dockerfile`·`railway.json` 은 디스크가 붙는 호스트용으로 남겨둔 대안이다. 현재 배포 경로가 아니다.

## 현재 진행 상황 (`osaka_travel_buddy`)

기획한 기능은 모두 구현됐다: 인증 · 링크 해석 · 저장목록 일괄 등록 · 장소 CRUD · 좋아요/확정/다녀옴/고정 · 휴지통 · 사진 프록시 · 목록 · 검색 · 필터 · 정렬 · 지도 · 관리자 패널(멤버·카테고리·지역) · 내 프로필.

**Vercel 에 배포되어 실제로 쓰이고 있다.** 운영 데이터는 Upstash Redis 에만 있고 저장소에는 없다 — 백업 명령이 `DEPLOY.md` 에 있다. `seed/db.json` 에는 계정 없이 카테고리·지역만 들어 있다(PIN 해시를 저장소에 올리면 4자리라 사실상 공개와 같다).
